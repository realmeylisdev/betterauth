# Dart pub workspace root. Not published; ties the packages together so they
# share a single resolution + lockfile during development.
name: betterauth_workspace
publish_to: none

environment:
  sdk: ^3.11.0

workspace:
  - packages/betterauth_dart
  - packages/betterauth_flutter
  - packages/betterauth_flutter/example
