include: package:very_good_analysis/analysis_options.yaml

linter:
  rules:
    # The example app is not a published library, so public API docs are not
    # required.
    public_member_api_docs: false
